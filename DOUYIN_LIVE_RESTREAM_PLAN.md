# 抖音直播 PAGE 解析与前端直拉方案

## 1. 当前链路

```text
用户进入本站直播间
  -> 本站接口校验直播任务
  -> 服务端 DouyinResolver 解析临时 HLS 地址
  -> 地址返回给前端
  -> 用户设备直接从抖音 CDN 拉取媒体
```

本站服务器不再使用 FFmpeg 拉取抖音媒体，也不再通过 SRS 二次分发抖音直播。服务器只承担房间管理、来源校验和播放地址解析。

## 2. 支持的来源

```text
https://live.douyin.com/
https://live.douyin.com/房间号
https://live.douyin.com/categorynew/分类号
https://live.douyin.com/hot_live
```

除 `live.douyin.com` 外，后台拒绝其他来源域名。

分类页发现房间时，任务会保存 `preferred_room_id`，保证每个本站直播用户对应固定的来源房间，不会在解析时随意串台。

### 女性来源硬规则

抖音分类页不提供可依赖的性别字段，因此系统禁止根据昵称、标题或头像自动猜测性别：

- 新发现房间统一进入待审核池，默认不能上线。
- 只有 `gender_tag = female`、`verify_status = 1`、`status = 1` 三项同时满足的人工确认房间才可接入。
- 可在后台按页勾选并批量确认女性、批量拒绝，或一次导入最多 1000 个已人工确认的女性房间号。
- 后台启动任务和客户端解析接口都会再次检查女性白名单，不能通过手工改任务绕过。
- 审核被撤销后，已有解析缓存也不再返回。

### 人工指定房间（推荐日常使用）

- 后台“创建虚拟直播”默认选择“抖音房间号（前端直拉）”。
- 管理员选择一个平台虚拟用户，填写纯房间号或具体直播间链接，再点“检测并开播”。
- 提交时自动规范为 `https://live.douyin.com/房间号`，并按管理员人工审核结果登记房间。
- 上线前必须同时通过：房间在线、解析房间号完全一致、HTTPS HLS 地址有效、视频清单可读取。任一项失败都拒绝上线，不会随机换到别的房间。
- 同一抖音房间号不能重复绑定多个正在直播的平台用户。
- 首次解析结果会预热 30 秒；H5 遇到网络或媒体错误先原地恢复，再最多 4 次向解析接口刷新同一房间的直拉地址。
- 女性白名单不足时保持少播或不播，禁止用未知性别或男性房间补位。

## 3. 服务端职责

- 只接受后台已创建、正在直播且标记为 `virtual_live_direct` 的房间。
- 根据本站主播 ID 与流名反查授权任务，前端不能提交任意第三方页面让服务器解析。
- 从抖音页面选择不高于 720p 的 HLS 地址。
- 解析结果短暂缓存 30 秒，降低页面解析频率。
- 前端播放失败时可强制刷新一次临时地址。
- 完整签名地址、Cookie 和令牌不写入日志。
- PAGE 直拉任务不创建 FFmpeg 进程，不向 SRS 推送媒体。

解析接口：

```text
Live.resolveSource
```

参数：

```text
liveuid  本站主播 ID
stream   本站直播流名
refresh  1 表示忽略缓存并重新解析
```

## 4. 前端职责

- 直播列表只保存本站房间资料和来源 PAGE，不保存长期媒体地址。
- 进入直播间后调用解析接口取得临时 HLS 地址。
- `<video>` / Hls.js 直接请求抖音 CDN，媒体请求不经过本站服务器。
- HLS 清单或视频播放出现致命错误时，前端延迟后重新解析，最多自动重试两次，避免无限请求。
- 用户手动点击“重试拉流”时可以再次解析。

## 5. 后台任务

PAGE 任务启动后：

```text
deviceinfo = virtual_live_direct
pid = 0
push_url = 空
command = 空
pull = 抖音来源 PAGE
isvideo = 1
```

任务状态表示“该房间已在本站上线”，不代表服务器存在媒体转推进程。

### 批量发现与批量更换

- 单次可发现、导入、审核或更换最多 1000 个房间。
- 默认并发目标为 300 路，可通过 `VIRTUAL_LIVE_MAX_ACTIVE` 调整，上限 1000。
- “一键批量更换”先一次发现当前在线房间，再整批下线未审核或已离线的来源，最后只从“在线且已确认女性”的交集中补足。
- 批量操作使用数据库互斥锁，避免两个管理员同时执行造成重复房间。
- 返回目标数、发现数、合格女性数、下线数、新增数、最终在线数和缺口数，白名单不足不会被当作需要放宽性别规则的理由。

后台按钮和定时任务使用同一套逻辑。命令行可执行：

```bash
php admin/scripts/batch_replace_virtual_live.php --target=300 --page=https://live.douyin.com/
```

## 6. 验收标准

- 后台只接受 `live.douyin.com`。
- 服务端能为已授权的本站房间解析当前可用的 720p HLS。
- 直播间视频的实际请求主机属于抖音/字节 CDN，而不是本站域名。
- PAGE 任务的 PID、推流地址和转推命令均为空。
- SRS 中不存在这些 PAGE 房间的 active 流。
- 系统中没有遗留的抖音拉流 Python/FFmpeg 进程。
- 临时播放地址过期后，前端能自动重新解析并继续播放。

## 7. 边界说明

直拉依赖来源 CDN 允许用户浏览器访问。如果抖音调整签名、跨域或防盗链策略，解析仍可能成功但浏览器拒绝播放；此时应更新解析或前端兼容逻辑，不能静默退回本站媒体转发，否则会违背“媒体不走服务器”的要求。
