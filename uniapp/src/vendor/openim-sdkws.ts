// @openim/client-sdk 3.8.3 imports this generated enum through a path that the
// matching protocol package only publishes as a declaration file.
export enum PullOrder {
  PullOrderAsc = 0,
  PullOrderDesc = 1,
  UNRECOGNIZED = -1
}
